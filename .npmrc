# React Native's Metro bundler doesn't play well with pnpm's strict node_modules
# layout (hoisting expectations). Use a flat node_modules tree so Metro can find
# transitive deps like react-native-css-interop (NativeWind) and worklets.
node-linker=hoisted
